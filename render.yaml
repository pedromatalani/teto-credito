services:
  - type: web
    name: teto-credito
    runtime: python
    buildCommand: echo "No build needed"
    startCommand: python3 server.py
    envVars:
      - key: PORT
        value: 10000
    disk:
      name: teto-data
      mountPath: /data
      sizeGB: 1
